syntax = "proto3";

option java_package = "com.liongjfuan.android_distribution.bean";
package bean;

message RegisterReq {
    int64 telephone = 1;    // 电话号码
    string password = 2;    // 密码
    string re_password = 3; // 密码确认
    string os = 4;  // os表示是在ios上使用还是在android上使用，os=ios或os=android
}
